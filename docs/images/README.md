# Reference Figures

Diagnostic and reference figures generated during pipeline development and
simulation audit. All figures use the SciencePlots `science + notebook` style.

## Array Geometry

| File | Description | Generated by |
|---|---|---|
| `dsa110_antenna_layout.png` | ENU layout of all 117 station positions. Core T-shape (EW arm + NS arm) clearly visible, with 14+ outriggers extending to ~1.8 km E-W and ~2.2 km N-S. | `scripts/plot_tile_image.py` |
| `dsa110_psf_analysis.png` | Synthesized beam analysis at Dec +16.15°, 1405 MHz. 6-panel: UV coverage, PSF wide view, main beam zoom, E-W slice, N-S slice, and parameter summary. Shows how T-core vs outrigger baselines shape the dirty beam. | `scripts/make_psf_analysis.py` |

**Key beam parameters (117 antennas, Dec +16.15°, 1405 MHz):**

| Parameter | Value |
|---|---|
| HPBW E-W (full array) | 75" |
| HPBW N-S (full array) | 332" (4.4× elongated) |
| Peak sidelobe | 0.415 |
| UV fill fraction | 0.90% |
| u range | ±48.9 kλ |
| v range | ±10.9 kλ |

## Pipeline QA Outputs (approved)

| File | Description | Stage |
|---|---|---|
| `step2_calibrator_visibility.png` | Calibrator visibility amplitude vs baseline length (all subbands). Confirms correct UV-distance scaling. | Step 2 |
| `step3_gain_solutions.png` | Per-antenna gain solutions (amplitude + phase) across all 117 antennas and 16 subbands. Confirms convergence of the calibration step. | Step 3 |

## Simulation Diagnostics

| File | Description |
|---|---|
| `simulation_diagnostic.png` | Early simulation diagnostic — visibility amplitudes and baseline coverage from initial 8-antenna prototype run. |
| `wsclean_dirty_image.png` | WSClean dirty image from early pipeline test run (pre-geometry-fix). Kept for comparison against corrected Step 4 output. |

## Regenerating figures

```bash
# Antenna layout
python scripts/plot_tile_image.py --layout-only

# PSF analysis (requires /tmp/uv_*.npy arrays — regenerated by harness if missing)
python scripts/make_psf_analysis.py
```
